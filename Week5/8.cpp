// ehllo       //sort(s.begin(), s.end() );
//  olleh        reverse(s.begin(), s.end() );
#include <iostream>
#include <algorithm>

using namespace std;

int main(){
    string s = "hello";
    //sort(s.begin(), s.end() );
    //reverse(s.begin(), s.end() );
    cout << s[0];
    return 0;
}