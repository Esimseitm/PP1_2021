#include <iostream>
#include <cmath>

using namespace std;

int main () {
    /*int n;
    cin >>n;
    //cout << sqrt(a);
    printf("sqrt of %d = %.2f" , n, sqrt(n)); // .2ф означает что после точки пишем всего 2 цифры. %Д осначает что сюда надо поставить следущее после запятой.
    return 0;*/

    int n;
    cin >>n;
    //cout << sqrt(a);
    printf("sqrt of %d = %.2f\n" , n, sqrt(n)); // .2ф означает что после точки пишем всего 2 цифры. %Д осначает что сюда надо поставить следущее после запятой.
    printf("%.10f", M_PI);
    return 0;
}